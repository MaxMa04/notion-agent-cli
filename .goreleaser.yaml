version: 2

project_name: notion-agent-cli

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X github.com/MaxMa04/notion-agent-cli/cmd.Version={{.Version}}
    binary: notion-agent

archives:
  - format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^ci:"
      - "^chore:"

brews:
  - repository:
      owner: MaxMa04
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://github.com/MaxMa04/notion-agent-cli"
    description: "Notion Agent CLI — full-featured CLI for Notion, built for AI agents."

scoops:
  - repository:
      owner: MaxMa04
      name: scoop-bucket
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: "https://github.com/MaxMa04/notion-agent-cli"
    description: "Notion Agent CLI — full-featured CLI for Notion, built for AI agents."
    license: "MIT"

nfpms:
  - homepage: "https://github.com/MaxMa04/notion-agent-cli"
    maintainer: "Max Mannstein"
    description: "Notion Agent CLI — full-featured CLI for Notion, built for AI agents."
    license: "MIT"
    formats:
      - deb
      - rpm

dockers:
  - image_templates:
      - "ghcr.io/MaxMa04/notion-agent-cli:{{ .Version }}-amd64"
    use: buildx
    build_flag_templates:
      - "--platform=linux/amd64"
    goarch: amd64

  - image_templates:
      - "ghcr.io/MaxMa04/notion-agent-cli:{{ .Version }}-arm64"
    use: buildx
    build_flag_templates:
      - "--platform=linux/arm64"
    goarch: arm64

docker_manifests:
  - name_template: "ghcr.io/MaxMa04/notion-agent-cli:{{ .Version }}"
    image_templates:
      - "ghcr.io/MaxMa04/notion-agent-cli:{{ .Version }}-amd64"
      - "ghcr.io/MaxMa04/notion-agent-cli:{{ .Version }}-arm64"

  - name_template: "ghcr.io/MaxMa04/notion-agent-cli:latest"
    image_templates:
      - "ghcr.io/MaxMa04/notion-agent-cli:{{ .Version }}-amd64"
      - "ghcr.io/MaxMa04/notion-agent-cli:{{ .Version }}-arm64"
